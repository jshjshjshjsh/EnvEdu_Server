version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/EnvEdu_Server

permissions:
  - object: /home/ubuntu
    pattern: "**"
    owner: ubuntu
    group: ubuntu

hooks:
  AfterInstall:
    - location: scripts/deploy.sh
      timeout: 100
      runas: ubuntu




