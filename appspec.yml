version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/EnvEdu_Server
    overwrite: yes

hooks:
  ApplicationStart:
    - location: /scripts/deploy.sh
      timeout: 60
      runas: ubuntu